export default class Path {
  constructor(from, direction, to, condition, alert) {
    this.from = from
    this.direction = direction
    this.to = to
    this.condition = condition
    this.alert= alert
  }
}
